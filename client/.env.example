# Leave unset in production builds (same-origin). Dev falls back to :3001 automatically.
# REACT_APP_API_BASE_URL=http://localhost:3001

# Clerk publishable key (dashboard.clerk.com -> API keys). Public by design.
REACT_APP_CLERK_PUBLISHABLE_KEY=pk_test_your-key
